# Python CI + SAST + dependency audit. Merge gate: ci / summarize.

name: ci

on:
  pull_request:
  merge_group:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: ci / test
    runs-on: ${{ (github.event.pull_request.head.repo.fork || vars.CI_FORCE_HOSTED == 'true') && 'ubuntu-latest' || 'moosegoose-general' }}
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10  # pin: actions/checkout@v6.0.3

      - uses: astral-sh/setup-uv@fac544c07dec837d0ccb6301d7b5580bf5edae39  # pin: astral-sh/setup-uv@v8.2.0
        with:
          enable-cache: true

      - name: Sync dependencies
        run: uv sync --extra dev --locked

      - name: Pytest
        run: uv run --extra dev pytest -q

  bandit:
    name: ci / bandit
    runs-on: ${{ (github.event.pull_request.head.repo.fork || vars.CI_FORCE_HOSTED == 'true') && 'ubuntu-latest' || 'moosegoose-general' }}
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10  # pin: actions/checkout@v6.0.3

      - uses: astral-sh/setup-uv@fac544c07dec837d0ccb6301d7b5580bf5edae39  # pin: astral-sh/setup-uv@v8.2.0
        with:
          enable-cache: true

      - name: Sync dependencies
        run: uv sync --extra dev --locked

      - name: Bandit SAST (scripts/)
        run: uv run --extra dev bandit -r scripts -ll -s B310

  pip-audit:
    name: ci / pip-audit
    runs-on: ${{ (github.event.pull_request.head.repo.fork || vars.CI_FORCE_HOSTED == 'true') && 'ubuntu-latest' || 'moosegoose-general' }}
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10  # pin: actions/checkout@v6.0.3

      - uses: astral-sh/setup-uv@fac544c07dec837d0ccb6301d7b5580bf5edae39  # pin: astral-sh/setup-uv@v8.2.0
        with:
          enable-cache: true

      - name: Sync dependencies
        run: uv sync --extra dev --locked

      - name: pip-audit (locked dev deps)
        run: uv run --extra dev pip-audit

  summarize:
    name: ci / summarize
    needs: [test, bandit, pip-audit]
    if: always()
    runs-on: ${{ (github.event.pull_request.head.repo.fork || vars.CI_FORCE_HOSTED == 'true') && 'ubuntu-latest' || 'moosegoose-general' }}
    steps:
      - name: CI gate
        env:
          TEST: ${{ needs.test.result }}
          BANDIT: ${{ needs.bandit.result }}
          PIP_AUDIT: ${{ needs.pip-audit.result }}
        run: |
          echo "## CI Summary"
          echo "| Job | Status |"
          echo "| --- | --- |"
          echo "| test | ${TEST} |"
          echo "| bandit | ${BANDIT} |"
          echo "| pip-audit | ${PIP_AUDIT} |"
          for status in "$TEST" "$BANDIT" "$PIP_AUDIT"; do
            if [ "$status" = "failure" ] || [ "$status" = "cancelled" ]; then
              echo "CI gate failed."
              exit 1
            fi
          done
          echo "CI gate passed."
