# Python CI + SAST + dependency audit. Merge gate: ci / summarize.

name: ci

on:
  pull_request:
  merge_group:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: ci / test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # pin: actions/checkout@v7.0.1

      - uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9  # pin: astral-sh/setup-uv@v9.0.0
        with:
          enable-cache: true

      - name: Sync dependencies
        run: uv sync --extra dev --locked

      - name: Pytest
        run: uv run --extra dev pytest -q

  bandit:
    name: ci / bandit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # pin: actions/checkout@v7.0.1

      - uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9  # pin: astral-sh/setup-uv@v9.0.0
        with:
          enable-cache: true

      - name: Sync dependencies
        run: uv sync --extra dev --locked

      - name: Bandit SAST (scripts/)
        run: uv run --extra dev bandit -r scripts -ll -s B310

  pip-audit:
    name: ci / pip-audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # pin: actions/checkout@v7.0.1

      - uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9  # pin: astral-sh/setup-uv@v9.0.0
        with:
          enable-cache: true

      - name: Sync dependencies
        run: uv sync --extra dev --locked

      - name: pip-audit (locked dev deps)
        run: uv run --extra dev pip-audit

  summarize:
    name: ci / summarize
    needs: [test, bandit, pip-audit]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: CI gate
        env:
          TEST: ${{ needs.test.result }}
          BANDIT: ${{ needs.bandit.result }}
          PIP_AUDIT: ${{ needs.pip-audit.result }}
        run: |
          echo "## CI Summary"
          echo "| Job | Status |"
          echo "| --- | --- |"
          echo "| test | ${TEST} |"
          echo "| bandit | ${BANDIT} |"
          echo "| pip-audit | ${PIP_AUDIT} |"
          for status in "$TEST" "$BANDIT" "$PIP_AUDIT"; do
            if [ "$status" = "failure" ] || [ "$status" = "cancelled" ]; then
              echo "CI gate failed."
              exit 1
            fi
          done
          echo "CI gate passed."
